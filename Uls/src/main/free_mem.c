#include "uls.h"

void mem_free(data_t *data, start_t *start_data) {
    // for (int j = 0; j < start_data->dirs_num; j++) {
    //     for (int i = 0; data[1][j][i] != NULL; i++) {
    //         free(data[1][j][i]->name);
    //         free(data[1][j][i]->date);
    //         free(data[1][j][i]->group_id);
    //         free(data[1][j][i]->user_id);
    //         free(data[1][j][i]->rights);
    //         free(data[1][j][i]->link);
    //         free(data[1][j][i]);
    //     }
    //     free(data[1][j]);
    // }
    // free(data[1]);
    // free(data);
}
